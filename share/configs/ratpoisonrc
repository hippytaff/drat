set border 0
escape C-X
bind t exec terminology # no-logo
bind C exec terminology -b /usr/share/backgrounds/drat-logo.png
bind c exec terminology -S vh -e mc $SHELL 'tty-clock -c'
bind B exec firefox
bind m exec terminology -e mc
bind i exec terminology -e irssi

# bind c exec rxvt -rv +sb -sl 1000 -font 9x15 -ls # Decent rxvt term if you prefer
# bind p exec firefox --remote "openurl(`$RATPOISON -c getsel`,new-tab)" # `C-t' : open link in new tab

